---
scope: In-house rich-text editor platform, React/Next.js integration, demo applications, testing, and release lifecycle
purpose: Build contract for developers and AI agents
altitude: feature
updated: 2026-09-15T22:03
---

- (direction) Architecture Spine เป็น build contract แบบกระชับสำหรับ developer และ AI agent
- (decision) เขียน editing kernel เองทั้งหมดบน browser primitives โดยไม่ใช้ ProseMirror, Lexical หรือ third-party editor kernel; ทีมเป็นเจ้าของ document model, transactions, selection mapping, history, input, composition และ clipboard behavior
- (constraint) React และ Next.js เป็น integration targets; ต้องมี demo ที่ใช้ทดสอบได้และ release lifecycle สำหรับ library packages
- (event) เริ่ม Coaching path โดยผู้ใช้เลือกตัดสินใจร่วมกันทีละประเด็น
- (decision by user) ใช้ Hexagonal Architecture ร่วมกับ Functional Core / Imperative Shell: document kernel เป็น pure TypeScript และ DOM, HTML, React, Next.js เป็น adapters ที่เชื่อมผ่าน ports
- (decision by user) ใช้ pnpm Workspaces และ Turborepo; packages ที่เผยแพร่อยู่ใต้ packages/* ส่วน React และ Next.js demo อยู่ใต้ apps/* และเป็น private
- (decision by user) Public packages ใช้ independent semantic versions ไม่บังคับออกเวอร์ชันพร้อมกัน; release tooling ต้องอัปเดต dependent package เมื่อ dependency range เดิมรองรับเวอร์ชันใหม่ไม่ได้
- (direction by user) เหตุผลหลักที่เลือก independent versions คือไม่ต้องการ bump package ที่ไม่ได้เปลี่ยน ไม่ใช่ความต้องการให้ consumer ใช้ internal layers แยกกันทั้งหมด
- (decision by user) Publish เฉพาะ product-boundary packages ได้แก่ editor-core, editor-react, editor-html, editor-checks และ editor-content; editor-dom และ editor-source เป็น internal workspace packages และห้าม consumer import โดยตรง
- (decision) ใช้ TypeScript strict และ tsdown สร้าง ESM-only packages พร้อม .d.ts และ source maps; ทุก package ใช้ explicit exports, externalize peer dependencies และผ่าน publint กับ packed-package smoke test
- (version) tsdown 0.23.x เป็นรุ่นที่ตรวจสอบจากเอกสารทางการ ณ 2026-09-15; implementation ต้อง pin exact version และ upgrade ผ่าน PR ที่รัน package compatibility checks
- (decision by user) สร้าง private demo apps สองตัว: Vite + React playground สำหรับพัฒนาและทดสอบ feature/fixtures และ Next.js App Router demo สำหรับตรวจ client boundary, CMS integration, server-rendered HTML และการไม่ส่ง editor runtime ไปหน้า public
- (decision) ใช้ Vitest ใน Node สำหรับ pure kernel, HTML และ content checks; ใช้ Playwright บน browser จริงสำหรับ DOM editing, selection, input, clipboard, composition, React scenarios และ Next.js integration
- (constraint) ทุก PR รัน unit suite และ Chromium browser suite; release gate รัน Chromium, Firefox และ WebKit; nightly รัน full fixtures และ randomized transaction tests
- (constraint) Thai IME มี automated composition-event regression suite และ manual acceptance checklist บน browser/OS จริง เพราะ synthetic automation ไม่พิสูจน์ native IME ได้ทั้งหมด
- (decision) Release เป้าหมายแรกเป็น open-source public npm packages; ยังไม่มี private package publication ใน initial architecture
- (constraint) Public scoped packages ใช้ publishConfig.access=public; demo apps และ internal workspace packages ใช้ private=true เพื่อป้องกัน accidental publish
- (direction) ใช้ npm registry เป็น publish destination ขณะที่ monorepo package manager ยังคงเป็น pnpm Workspaces
- (decision) ใช้ Changesets release PR และ GitHub Actions release pipeline พร้อม GitHub Environment human approval ก่อน npm Trusted Publishing; สร้าง provenance, git tags และ GitHub Release
- (constraint) Release gate ต้องผ่าน typecheck, lint, unit tests, Chromium/Firefox/WebKit suites, React/Next.js production builds, packed-tarball clean-consumer smoke tests, publint, exports validation, bundle-size threshold และ HTML compatibility fixtures
- (decision) MVP รับประกัน editing บน Chrome/Edge, Firefox และ Safari รุ่น stable ปัจจุบันบน desktop; mobile editing ไม่อยู่ใน support contract ระยะแรก
- (constraint) HTML output และ responsive content stylesheet ต้องแสดงผลบน mobile ได้ แม้ editor บน mobile จะยังไม่รับประกัน
- (direction) เปิด mobile editing เมื่อ desktop compatibility suite เสถียร และมี story สำหรับ touch selection, virtual keyboard, mobile clipboard, IME และ responsive toolbar โดยเฉพาะ
- (decision) editor-react รองรับ React และ React DOM ตั้งแต่ 18.2 จนก่อน 20 ผ่าน peerDependencies; CI ต้องทดสอบ React 18.2 และ React 19
- (constraint) editor-react ห้ามพึ่ง public API ที่มีเฉพาะ React 19 จนกว่าจะยกเลิก React 18 support ผ่าน major release
- (constraint) Next.js เป็น integration target และ demo dependency ไม่ใช่ peer/runtime dependency ของ public library packages
- (decision) HTML security ใช้ shared ContentPolicy เป็น source of truth แล้ว compile เป็น DOMPurify configuration ฝั่ง browser และ sanitize-html configuration ฝั่ง server
- (constraint) Security fixtures ชุดเดียวต้องผ่าน sanitizer adapters ทั้งสอง; semantic safe output ต้องเทียบเท่ากัน แต่ไม่รับประกัน byte-identical HTML
- (constraint) Sanitizer เป็น dependency ที่อัปเดตด้านความปลอดภัยโดยเฉพาะและไม่เป็นเจ้าของ document parser/serializer contract; server-side sanitization ยังคงเป็น security boundary
- (decision by user) EditorEngine instance เป็นเจ้าของ document state, selection, history และ revision; React adapter อ่าน immutable UI snapshot ผ่าน subscribe/getSnapshot และ useSyncExternalStore โดย core ห้ามขึ้นกับ React หรือ third-party state manager
- (decision) editor-react ส่ง headless hooks/context, accessible UI primitives และ optional default controls/styles โดยไม่พึ่ง visual component framework; consumer สามารถแทน toolbar/menu/style ได้ทั้งหมด
- (constraint) editor.css เป็น optional CMS UI stylesheet ส่วน editor-content/content.css เป็น public rendered-content contract ที่ CMS preview และเว็บไซต์ใช้ร่วมกัน
- (constraint) Modal lifecycle เป็นของ consumer; library เปิด selection bookmark, command state และ typed dispatch APIs เท่านั้น
- (constraint) UI class naming แยก namespace จาก persisted content token classes และ built-in controls ต้องผ่าน WCAG 2.2 AA keyboard/focus semantics
- (decision) ใช้ release channels canary/snapshot สำหรับ PR testing, beta สำหรับ release candidate และ latest สำหรับ stable release ที่ผ่าน human approval
- (constraint) ก่อน 1.0 breaking changes ต้องมี Changeset และ migration note; ตั้งแต่ 1.0 ใช้ SemVer เคร่งครัด โดย HTML contract, persisted class names และ plugin API เป็น compatibility surface
- (constraint) Demo consumer smoke tests ต้องติดตั้ง packed tarball หรือ canary package ไม่อาศัย workspace source resolution อย่างเดียว
- (decision) Source repository อยู่บน GitHub และใช้ GitHub Actions กับ GitHub-hosted runners สำหรับ CI/release
- (question) npm organization/scope ยังไม่มีและต้องกำหนดก่อน first public publish; ระหว่างพัฒนาใช้ logical package directories โดยไม่ฝัง scope สมมติใน public documentation
- (constraint) First-publish checklist ต้องตรวจ npm organization ownership, package-name availability, trusted publisher mapping และ repository URL ให้ตรงกันก่อนเปิด release
- (decision by user) เผยแพร่ source code และ public npm packages ภายใต้ MIT License; first release ต้องมี LICENSE, package license metadata และ third-party notices ที่จำเป็น
- (decision) ใช้ TypeScript 7.x native compiler สำหรับ greenfield codebase; pin minor line และอัปเกรดผ่าน dependency PR ที่รัน full gate
- (constraint) Production packages ห้ามใช้ TypeScript compiler API; declaration build ต้องผ่าน tsdown และ packed-consumer type tests บนทุก release
- (version) TypeScript 7.0.2 ตรวจสอบจาก npm registry และประกาศทางการ ณ 2026-09-15
- (decision) Public Node-capable packages รองรับ Node.js 22 และ 24; engines.node กำหนด >=22 และการยกเลิก Node 22 ต้องเป็น major release
- (constraint) CI build/release ใช้ Node 24 ส่วน packed-package smoke matrix รัน Node 22 และ 24
- (version) npm Trusted Publishing ต้องการอย่างน้อย Node 22.14 และ tsdown 0.23 ต้องการ Node 22.18; เลือก Node 24 เป็น release runtime
- (decision) HTML parsing ใช้ native DOMParser/template adapter ใน browser และ parse5 adapter บน server; ทั้งสองแปลงเป็น shared normalized HTML AST ก่อนเข้าสู่ document model
- (constraint) Canonical HTML ต้องมาจาก pure deterministic serializer ของเรา ห้ามใช้ browser innerHTML เป็น persisted output; cross-parser fixtures ต้องพิสูจน์ semantic equivalence
- (version) Verified stack snapshot 2026-09-15: Node 24 release runtime with Node >=22 support; TypeScript 7.0.2; React 19.3.0 demo with peer >=18.2 <20; Next.js 16.3.5; Vite 8.3.0; pnpm 12.4.1; Turborepo 2.10.13; tsdown 0.23.0; Vitest 5.0.0; Playwright 1.63.0; Changesets 3.0.3; DOMPurify 3.4.15; sanitize-html 2.17.7; parse5 8.0.1; publint 0.3.24; @arethetypeswrong/cli 0.18.5
- (question) npm organization/scope และ final package names deferred จนก่อน first publish เพราะยังไม่มี organization
- (question) Image upload API contract, legacy TinyMCE inline-style mapping, block-background semantics และ allowed media hosts เป็น capability-level decisions ที่ต้องปิดใน stories ก่อนสร้าง feature นั้น ไม่ขวาง repository scaffold
- (direction) Lint/format implementation tool และ dependency-update bot เป็น replaceable repository tooling seed ให้เลือกตอน scaffold โดยต้องมี deterministic CI commands; ไม่เป็น architecture invariant
- (decision) RawHtmlNode ต้องรักษา safe unsupported subtree แบบ lossless และ read-only ใน visual mode; canonical normalization ใช้เฉพาะ structured supported content
- (constraint) Source Apply ต้อง validate syntax และ policy แล้ว reject ทั้ง operation เมื่อไม่ผ่าน ห้าม sanitize/แก้ source เงียบ ๆ; sanitizer ใช้กับ untrusted render/paste/preview boundary ตาม contract
- (constraint) Source mode ต้องถูกเปิดโดย host authorization, ใช้ isolated working copy, Cancel ไม่เปลี่ยน state, Apply สำเร็จเป็น transaction เดียวที่ undo ได้ และ failure ไม่เปลี่ยน document/history
- (constraint) Dirty state เทียบกับ host-acknowledged saved HTML snapshot และกลับ clean เฉพาะเมื่อ host ยืนยัน baseline ใหม่
- (constraint) Content check results ผูกกับ document revision; adapter ต้องทิ้งหรือ rerun stale results และ optional fix ต้อง dispatch command กับ revision/target ที่ยังใช้ได้
- (decision) editor-core เป็น canonical owner ของ cross-package model/selection/transaction/event/plugin/token-registry types; editor-html เป็น owner ของ normalized HTML AST, diagnostics และ ContentPolicy
- (decision) ModelPosition ใช้ nodeId, offset, affinity; committed transaction เพิ่ม monotonic revision ครั้งเดียวและ publish snapshot ตาม commit order; async acknowledgements/results ต้องมี revision และ normalized HTML digest
- (constraint) editor-dom เป็น browser mutation owner เพียงรายเดียว; React/plugins ห้ามแก้ editable subtree และ composition buffer commit เข้า core ตาม explicit lifecycle
- (constraint) HTML/CSS rollout deploy renderer stylesheet และ sanitizer support ก่อนเปิด serializer output ใหม่; rollback ต้องอ่าน persisted contract รุ่นปัจจุบันได้
- (decision) Independent-version release ใช้ compatibility cohort: publish immutable versions ใต้ temporary dist-tag, smoke test ทั้ง cohort แล้วจึง promote latest; failure คืน dist-tags ไป last-known-good และ forward-fix
- (version) Reviewer-verified updates: pnpm 12.4.2, Vitest 5.0.1; ใช้ Changesets CLI 2.31.1 คู่ stable changesets/action v1.9.0 สำหรับ version PR เท่านั้น เพื่อหลีกเลี่ยง action v2 ที่ยังเป็น development
- (constraint) Root toolchain minimum Node 22.18.0; public repository เป็น precondition ของ provenance และ GitHub Environment availability ต้องตรวจตอน scaffold
- (decision) Token registry เป็น immutable engine configuration owned by editor-core; editor-content provides optional preset data/CSS only. classPrefix และ token IDs/aliasesมี validation และ collision rules เดียว
- (decision) Model offsets are UTF-16 code-unit offsets for text and child indexes for containers; affinity is forward/backward. Node IDs are unique within one document lineage, preserved for unchanged/mapped nodes, regenerated on import, and never persisted
- (constraint) editor-dom owns composition lifecycle: buffer during active composition, suppress duplicate beforeinput commits, dispatch one core transaction on compositionend, and discard/restore explicitly on cancellation
- (decision) RawHtmlNode captures the topmost safe unsupported subtree as an opaque canonical fragment preserving elements/attributes/content semantically; descendants are not partially parsed, unsafe subtree rejects the operation
- (constraint) Stateful editor-core is externalized and declared as peer dependency by editor-react to prevent duplicate runtimes; editor-html exposes explicit shared/browser/server subpaths whose module graphs cannot cross environment dependencies
- (constraint) Stable and prerelease packages declare enforceable dependency/peer ranges; prerelease internal dependencies pin exact cohort versions. Promotion order may be non-atomic but every intermediate resolution must satisfy tested ranges; rollback restores tags only to range-compatible versions
- (constraint) Host must sanitize untrusted HTML at save acceptance and immediately before server render; security operations include advisory monitoring, emergency patch release, affected-version deprecation and forward-fix
- (decision) Built-in checker runs and image insertion receive explicit typed success/failure/status contracts tied to revision; decorative images require explicit intent and non-decorative images require alt text
- (decision) Selection affinity semantics: insertion at exact boundary moves forward-affinity position after inserted content and keeps backward-affinity before it; deletion maps deleted positions to nearest surviving boundary in affinity direction
- (constraint) blur, new non-composition input, or editor disposal while composing must synchronously finalize valid composition buffer exactly once; invalid buffer cancels, restores committed DOM, and emits diagnostic
- (decision) editor-html owns pure authoritative ContentPolicy evaluator and canonical policy fingerprint; sanitizer output must be reparsed and pass evaluator before trusted use
- (decision) Plugin registry has deterministic collision/precedence/convergence: unique IDs, explicit priority for overlaps, fixed order, exactly one serializer per node type, normalization fixed-point with cycle/limit rejection
- (constraint) Checker evaluation exposes pass/fail/manual-review/not-evaluated per rule; RawHtmlNode cannot silently pass and contrast must cover each registered theme/breakpoint variant or report not-evaluated
- (event) Architecture Spine finalized after deterministic lint, spec reconciliation, current-stack review, and adversarial closure reviews
