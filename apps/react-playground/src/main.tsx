import { createRoot } from 'react-dom/client';
import 'editor-content/content.css';

createRoot(document.getElementById('root')!).render(
  <main>
    <h1>Editor playground scaffold</h1>
    <p>Feature scenarios arrive in later stories.</p>
  </main>,
);
